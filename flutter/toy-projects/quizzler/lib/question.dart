class Question {
  // Non-nullable instance filed must be initialized.
  String qusetionText;
  bool questionAnswer;

  // The parameter 'q' can't have a value of 'null' becaue of it's type
  Question(this.qusetionText, this.questionAnswer);
}
